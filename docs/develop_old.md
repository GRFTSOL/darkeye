



只有静态的数据，比如很多的女优都会整容，变胖或者变瘦，前中后期整个脸都变了，男优的跨度太长了，演了20年的都有
静态数据分析的都没有什么意义，至少个人喜欢的和数据评分，身高的关联性都不太大。但是就是要脸好看，妆容好，还有很多无形的，比如演技，叫声，体位的色情程度，眼神，这个就很难分析。
很多的时候可能整体的和导演的关系很大，包括背景，机位，故事

# 文档工具的使用

mkdocs serve 启动然后点击链接

mkdocs build 构建


# 项目整体的结构
AVmanagement_project/
├── main.py                  # 入口，启动 QApplication 和主窗口
├── config.py                # 配置与路径管理（兼容 PyInstaller）
├── settings.ini             # INI 配置文件,这个不跟踪，由程序生成
├── requirements.txt         # 运行环境的要求
├── README.md                # 基本的文档说明
├── main.spec                # pyinstaller打包配置文件
├── mkdocs.yml               # mkdocs文档的配置


├── controller/          #控制
│   ├── __init__.py
│   ├── GlobalSignalBus.py       #全局信号
│   └── MessageService.py        #信息框弹出

├── scripts/                   #使用脚本
│   ├── build-nuitka.ps1
│   ├── build-pyinstaller.ps1   # powershell，构建项目的命令行
│   ├── cloc_by_file.py
│   └── clocCode.ps1           # powershell，看项目有多少行代码的命令行

├── core/ 
│   ├── __init__.py
│   ├── chart/                  # 绘图
│   ├── crawler/                # 爬虫
│   ├── database/               # SQLite 操作封装
│   ├── recommendation/         # 推荐算法

├── ui/                      # UI 构造模块（可以手写或加载 .ui 文件）
│   ├── __init__.py
│   ├── main_window.py       # 主窗口逻辑
│   ├── widgets/      # 自定义控件
│   ├── statistics/   # 统计表控件
│   ├── pages/        #页面    
│   ├── basic/        #基本的控件    
│   ├── base/         #基础  


├── resources/            # 图片、图标、数据库等静态资源,是需要复制到打包后的文件夹里的
│   ├── icons/            #内部的图标
│   ├── public/
│   │   ├── actressimages/  #女演员头像
│   │   ├── actorimages/    #男演员头像
│   │   ├── workcovers/     #作品的封面
│   │   └── public.db        #公有数据库   
│   ├── private/            
│   │   └── private.db      #私有数据库
│   ├── config/
│   │   ├── sesitive_words.txt  #敏感词过滤表
│   │   ├── tag_map.json        #文字转换标签表
│   └── sql/              #sql语句

├── tests/                   # 单元测试

├── docs/                             # 文档
│   ├── api.md
│   ├── av.md
│   ├── AV数据库概念ERD.excalidraw
│   ├── bug.md
│   ├── data-collect.md
│   ├── db-design.md
│   ├── develop.md
│   ├── future.md
│   ├── index.md
│   ├── recommendation.md
│   ├── ui-design.md
│   ├── usage.md
│   └── 软件架构.excalidraw



# 项目定位
本地化、隐私导向的 AV 领域元数据管理与情报分析系统。目标是收藏与找出最有艺术性的作品。说实话大部分作品都是垃圾。由于独特的性癖，这个定制化程度非常的高。

- Insight (洞察) ：不仅仅是存数据，而是发现数据背后的联系（如：挖掘“共演关系”、“新人出道轨迹”）。
- Immersive (沉浸) ：从“填表单”转变为“刷流媒体”般的体验。
- Zero-Friction (零摩擦) ：极致的自动化采集，让用户感知不到录入过程。

核心功能矩阵：
1. 数据采集与清洗 (Intelligence Gathering) ：
   - 多源爬虫 ：集成了 JavBus, JavDB, Minnano, Fanza 等多个数据源的爬虫 ( core/crawler )。
   - 浏览器协同 ：通过 Firefox 插件 ( extensions/firefox_capture ) 与本地服务器 ( server/app.py ) 联动，实现了“浏览即采集”的高效工作流。交互能力，在浏览器中就知道这个片子本地是否收藏过
   - ID 解析与清洗 ：具备处理复杂番号、去重的能力。
2. 档案管理 (Asset Management) ：
   - 结构化存储 ：公有库管理“女优 (Actress)”和“作品 (Work)”，“男优 (Actor)”，“标签”等等核心实体，及其关联关系。私有库关系喜欢的女优和作品，包括个人数据例如撸管记录。
   - 开放导出，所有的数据都可自由导出为CSV格式。
3. 可视化与洞察 (Visualization & Insight) ：
   - 关系图谱 ： ForceDirectedView (力导向图) 是产品的核心差异化亮点，用于展示人物与作品、人物与人物之间的关联网络。
   - 多维视图 ：包含时间轴 ( TimeLinePage )、画廊 ( GalleryPage ) 和列表视图。

量化自我 (Quantified Self) —— 贤者模式报告 ：
- 改进 ：不要只记录“看了什么”，要分析“口味偏好变化”。生成年度/月度报告：
  - "你这个月偏爱 MILF 类型，比上个月增加了 30%。"
  - "你在周五晚上的活跃度最高。"
- 价值 ：利用数据可视化（ForceView 之外的图表），让用户通过数据重新认识自己的性癖好。这能带来极强的情感连接。
隐私向，一键伪装功能，按下快捷键，界面变成Excel界面

独特UI风格，为CJK用户设计，展示页独特的竖排文本，体现CJK文化。极致的文化契合度 ：日文和中文（尤其是繁体）在竖排时能传达出强烈的“文学感”、“高级感”和“传统美学”。对于展示日系作品（AV 元数据），这能带来原汁原味的沉浸体验，仿佛在阅读日式海报或杂志。

i18n,目标支持多语言，包括简中、繁中、日文、韩文。

痛点 ：
- 工具属性过重 ：目前的 UI 逻辑偏向“数据库增删改查”工具，缺乏“消费内容”的沉浸感。
- 交互割裂 ：桌面端和浏览器端虽然有连接，但体验上是“采集 -> 传输 -> 刷新查看”，不够流畅。
- 录入繁琐 ： 用户更期待“一键完成”。

引入“Inbox（收件箱）”概念 ：
- 浏览器插件采集的数据不再直接入库，而是进入“待处理区”。
- 桌面端显示一个类似 Trello 的看板或流式列表。
- 智能预处理 ：系统自动后台跑完爬虫、人脸识别、NSFW 检测。用户只需要做最后一步：“确认归档”。


3. 插件与桌面端的无缝融合 (Seamless Sync)
现状 ：插件发送 HTTP 请求，桌面端被动接收。 优化方案 ：
- 实时双向反馈 ：
  - 当插件在网页上检测到页面（如 JavDB）时，不仅显示“可抓取”，还应通过 API 查询本地库，直接在网页上显示：“本地已存在 (已看/未看)”。



# 插件化架构
核心功能，包括数据仓库的管理，模型的定义，图系统绘制，雷达图，不依赖爬虫的增删查改的界面

插件化，matplotlib绘图，用爬虫的操作，机器学习的



# 功能开发




## 📌拟物类作品的收藏场景

注重仪式感，注重收藏感，有封面就包装成拟物的实体，放在书架子上，然后鼠标划过时可以抽出，然后观看。

黑化的番号复制按钮📌比如说什么什么大楼，几号位置等等，不容易被检测出来。

## 添加女优界面
- [x] 输入中文名与日文名，加女优，然后去网站自动更新信息

## 🔴 手动添加/修改作品界面
### 使用场景
可能在逛某地方的时候突然看到一个片子的番号只是单纯的想记录
单独添加一个番号，后面手动补信息

### 功能
- [x] 输入番号，发布日期，一句话剧情的输入框
- [x] 过滤信息库匹配显示的导演输入框
- [x] 详细的输入框中日标题与介绍内容系统
- [ ] 旁边有爬虫按钮可实时去抓取📌 
- [x] 可筛选的女优选择器
- [x] 可筛选的男优选择器
- [ ] 添加侧双击女优可输入更多的信息，包括女优的态度的转变，职业，年轻程度 📌
- [x] 保存临时修改与加载临时修改
- [ ] 近期的修改记录显示，根据时间线去搜
- [x] 修改的加载与存储
- [x] 封面的加载，以及拖动保存图片到库内位置
- [x] 内容更改检测，更好的提示，规范化能否添加修改的行为。
- [x] Tag页面合并到此处，并且完成加载，修改，内容更改检测，临时存储，与临时加载功能。
- [ ] 动态的tagwidget页面
- [ ] 对比功能，比如同故事的作品对比，主要是筛选出同类型最好的片子
- [x] 谷歌翻译，可将日文翻译成中文。
- [x] 一键打开网站，利于手动复制资料
- [x] 取消QTextEdit的文本格式
- [ ] 根据时长弄一个进度条，可以在上面插内容，比如什么场景，然后分段，等等。

### 技术实现
QDialog 弹出界面
- [x] 可筛选的选择器 
- [ ] 图片数据库image_url完整性检查📌
.ini保存内部的记录

近期的修改记录显示主要是依靠库内的时间戳，还是应该是外面的手动记录10条到.ini内

## 🌱 tag筛选界面 
- [x] tag选进去，就把作品给筛选出来，包括封面等


## ⏳ 封面显示作品与信息滑动组件
- [ ] UI优化，包括字体大小变化，但是不需要什么响应式的布局了，因为这个屏幕不会缩小到非常的小的。
- [x] 左右按钮滑动图片与信息加载
- [x] 女优与标签的定宽度自动换行的Flowlayout实现
- [x] 女优可点击跳转到对应单个女优页
- [ ] tag可点击跳转到tag筛选界面

## ⏳ 封面查找按钮
- [ ] QLineEdit信息模糊匹配显示
- [ ] 搜索后判断到底查了什么东西并跳转，番号跳作品，女优跳女优，精准匹配直接跳转，否则转成多搜索页面

## 🔥 作品添加/修改tag功能

### 使用场景
一般是看完片后添加tag,一般信息网站的tag非常的不准，几乎没有任何的参考价值，也没有任何的筛选价值

### 功能
- [x] 选择作品 
- [x] tag左右移动的选择内容器，左边一容器，右边一个toolbox分类标签
- [x] 互斥标签的锁的实现
- [x] 修改的加载与存储
- [x] UI的更改，更加的日式，比如标签是竖向的，然而这个要更改flowlayout,
- [ ] 近期的修改记录显示,这个要改数据库
### 技术实现
从数据库加载tag
自定义flowlayout+toolbox 多分组选择，点击tag后移动，tag内附带数据

## 🟢 管理tag功能 
实际上更改tag的比例是非常的低的
长按进更改模式，所有标签都在抖动，拖动排序tag_type组，点击更改标签内容
- [x] 单个更改tag的标签内容
- [ ] 更改标签互斥组-选中一堆，然后更改成一组
- [x] 更改标签颜色
- [ ] 更改标签的tag_type组
- [ ] 添加新的tag_type组
- [x] tag_type组在toolbox显示前后问题
- [x] 批量更改标签的颜色，选中一堆，然后更改成一种颜色
- [ ] 标签重映射问题，还剩下换主标签的功能
- [x] 取色表盘
- [ ] 合并标签的问题
- [ ] 高级标签过滤器支持与或非
tag模式，语素组成与标准化剧情
要不要互斥组，感觉这个东西很没用，完整性靠其他的来支持就行了

连锁tag

## 修改作品的tag功能 优先级中
- [ ] 选中作品然后加载对应的标签，然后和添加标签的界面就一样了

## 自动获得作品女优信息功能
[[data-collect]]

## 自慰记录的添加
一次撸管只记录一部片子
- [x] 番号搜索器，输入后匹配库作品
- [ ] 时间输入，额外预测自慰时间,这个没什么用，一般来说撸完添加
- [x] 评分系统1-5 爱心显示
- [x] 使用工具，工具库搜索不重复的
- [x] 事后评价框
- [x] 提交按钮
QDialog弹出
## 做爱记录的添加
- [x] 时间
- [x] 评分
- [x] 事后评价
- [x] 提交
## 晨勃记录的添加
这个有个问题，不及时的记录就会忘记，除非有东西一直的提醒你。至少晚上是想不起来早晨有没有晨勃的。
- [x] 时间
- [x] 事后评价


## 界面美化问题
加载界面的美化，需要日式的一种加载界面
各种东西需要美化，包括选择器等等



## 高分屏适配问题

响应式UI与动态布局


## 其他的简单功能
番号黑话生成分享📌
情趣体位抽签工具📌

## 多言语切换

## 亮暗主题切换

## 字体设置
找一个比较好的自己弄上去

# 测试用例
根目录执行
pytest


公共数据
私密数据
可成为公共数据的个人数据，比如个人的打标签，这需要每个人来参与

各种页面的截图分享机制用快捷键C


控制clash RESTful API  达到爬虫可用的目的
https://clash.gitbook.io/doc/restful-api

软件开始时数据库检查，无数据库建立数据库，但是这个是后面的事情，如何启动软件


软件设计时焦点的细节考虑

- [x] 界面懒加载
- [ ] 绘图迁移，不要用matplotlab,使用QChart绘图，好像交互性更加的好
- [ ] 绘图，横轴近30天，纵轴是24小时，然后绘撸管点图
- [ ] 合并姓名链条的问题，有脏数据污染怎么处理，还是规定只能在链条下
主要随着时间的发展，女优会改名字，所以还要弄一个可视化可以拖动的的界面处理链条问题

- [ ] 推动UI与操作层分离，每个界面下面专门弄一个initUI的函数，当然UI内部如果要计算的就在内部，一行程度的封装下分离

- [ ] 增量备份图片的问题

- [ ] 双模型方案，数据库模型与UI有个问题，如何能在保留用户编辑的情况下，把新的数据同步进来。包括乐观锁与悲观锁的问题。
在同时修改同一个东西时，没有任何的机制能知道保留哪个，只能提示覆盖，放弃，或者手动合并

- [ ] 女优，男优，导演，单部作品的.md相互串联的故事，非结构化的数据

作品女优relation 额外内容未实现
作品的          增删改查          交互UI
tag的           增删查改          交互UI
女优与女优姓名   增删查改          交互UI
男优与男优姓名   增删查改          表视图

前缀表与公司表的 增删查改         表视图
单体服饰表       增删查改         表视图
做爱动作表       增删查改         表视图

喜欢的作品      增删查改          爱心+过滤作品
喜欢的女优      增删查改          爱心+过滤女优
自慰记录表      增删查改           表视图
做爱记录表      增删查改           表视图
晨勃记录表      增删查改           表视图

科普页面就是markdown渲染器+双链跳转功能+node点图

分层架构

包括模型类与仓库类，然后在UI层MVVM 专门为UI的模型
就目前来看直接sqllite读写就行了，不需要专门弄模型类与仓库类，等到表再多些，到20张表后弄成模型类，50张表后上ORM框架

# 市场分析

同类产品，开源的，还有各种AV网站，AV网站根本就不会停留，因为老是换，AV网站的盈利模式基本靠赌博广告。

刮削器https://github.com/Yuukiy/JavSP

女优头像https://github.com/gfriends/gfriends?tab=readme-ov-file

管理器https://github.com/4evergaeul/JAV_MovieManager
https://github.com/elonmusx/avbook?tab=readme-ov-file

就目前jithub上的项目最多的是刮削器，然后是管理器，但是刮削器有个问题，只搜，不整理，没有形成有用的数据，刮出来的东西对个人来说没有价值


最后一定要接上各类刮削器，也就是爬虫

- [x]javtext最受欢迎的女优添加
- [ ]复制作品的全部tag到另一部作品中去
- [x]分解标题，添加tag 比如标题中有内射，添加中出标题,用词组分解，就是分解token的东西分解
- [ ]tag的操作，包括分裂，合并，运算，条件运算，等等
第一层简单的系统
[ 必须包含 ]   tagA  tagB
[ 可选满足 ]   tagC  tagD
[ 必须排除 ]   tagE
然后是复杂的表达式



现在的问题是如何处理片子之间的关系？主要是同一个企划的片子怎么分到一起，还有就是大致同样的故事，但是不同片商拍的也分到一起，这一点上missav做的很好

提纯管道



如何处理好这个收藏软件与AV网站的关系
AV网站作为一种热门与信息获取的来源，收藏软件作为一种永久性的，自由的，分享类的。所以需要快速的到达那些AV网站，也就是说爬虫策略与跳转策略需要外挂，可以快速的更新
作品与作品之间网状结构的可能性，用力导向图，要求力导向图可交互，包括拖动，放大缩小，调整吸力斥力的大小，然后点击可以跳转，

# 未来功能
插件化跳转
插件化爬虫，因为爬虫容易失效，这种事情一定要插件化，可供别人快速使用，包括插件化爬虫的开发问题。

与各种的刮削器对接，但是很多的刮削器非常的复杂，没有必要弄这么多的东西，简单的裁剪，刮削器就是刮来脏数据



包括作品，女演员，男演员，标签，导演

数据完整性问题，如何将不完整的信息给暴露出来，然后人工去修复

最高完整性，番号-导演-女演员-男演员-剧情-标题


如果不能上fanza通过第三方网站的爬虫的稳定性有很大的问题

最终的目的是寻找关系，比如这个片是谁演的，她还演过哪些，题材等等



- [ ] 一个备份系统，包括图片的备份，也就是说需要一个快照实现




### 1. 架构层面 (Architecture)
目前项目采用的是 Monolithic (单体) 架构，混合了 MVC 和 MVVM 模式。

- 问题 1：MainWindow 职责过重 (God Class)
  
  - 现状 ： MainWindow 类（ ui/main_window.py ）承担了过多的职责，包括路由跳转、信号连接、服务初始化（ TaskManager , StatusManager ）、全局快捷键注册等。
 

    - 依赖注入 (Dependency Injection) ：目前服务是在 MainWindow 内部直接实例化的。建议引入一个简单的 ServiceLocator 或 Context 类，负责管理 StatusManager 、 TaskManager 、 DatabaseManager 等单例服务， MainWindow 只需向容器请求即可。
- 问题 2：数据访问层 (DAL) 耦合
  
  - 现状 ： core/database/query.py 中充斥着大量的原始 SQL 拼接函数，且直接返回 dict 或 tuple 。虽然有 repositories/WorkRepository.py 文件，但似乎为空或未被充分利用。
  - 优化建议 ：
    - 完善 Repository 模式 ：将 query.py 中的函数迁移到对应的 Repository 类中（如 WorkRepository , ActressRepository ）。
    - 引入实体模型 (Domain Models) ：不要在 UI 层直接操作数据库返回的字典。建立明确的 Python 类（如 class Work , class Actress ）或使用 dataclasses / pydantic ，确保类型安全和代码提示友好。

- WorkRepository
- ActressRepository
- ActorRepository



### 3. 代码质量与规范 (Code Quality)
- 规范化配置管理 ：
  - 代码中存在硬编码的样式和颜色（如 #F7E6B0 , #282c34 ）。建议建立 ui/theme.py 或 resources/styles/theme.json 统一管理颜色和尺寸常量，方便后续支持“暗黑模式”或换肤。
- 异常处理 ：
  - core/database/query.py 中的 SQL 执行部分虽然使用了 with 语句管理连接，但部分查询缺少细致的 try-except 捕获，一旦 SQL 报错可能会导致程序崩溃或状态异常。
- 类型提示 (Type Hinting) ：
  - 部分新代码（如 QSqlDatabaseManager ）使用了 Type Hints，建议推广到全项目，特别是核心逻辑部分，有助于减少运行时错误。



2. P1 (中优先级) ： 重构数据层 。停止在 query.py 中写裸 SQL，迁移到 Repository 模式，并返回对象而非字典。
3. P2 (低优先级) ： UI 样式统一 。提取颜色和样式常量，规范化 QSS 管理。


你这个要求的关键点其实不是“性能”本身，而是： <br/> 加/减点和边之后，原有布局基本不动，只在局部微调 。 <br/> 这一点决定了我们必须做“增量维护”，而且还要让 SimulationState 里的数据结构支持长期演化，而不是每次重随机化。

我分三个层次说一个具体、可落地的设计思路（不写代码，只讲结构）。


# 图的整体架构


[数据库/文件] 
      ↓ (加载)
[GraphManager (总图 G)]  <-- (存储所有节点坐标 pos) <---------+
      ↓                                                  |
[过滤逻辑 (Filter)] (例如：剔除孤立点)                     |
      ↓                                                  |
[Subgraph (子图 G')] (仅包含可见节点，携带 pos)             | (位置同步)标按原来的，没有位置的坐标随机化                               |
[ForceView ] -----------------------------------+
      ↓
[ 物理引擎 ]
      ↓ (加载到视图)有位置的坐
(只对 G' 中的节点进行斥力/引力计算)
(隐藏的节点不参与任何运算，物理上消失)


markdown的编辑功能，包括作品的附加信息的编辑，女优的附加信息的编辑
QTextEdit + QSyntaxHighlighter + 事件过滤器 。


现在可以做一个类似进度条的功能，用来标记影片的位置干了什么兴奋的事情

- 第一步（最重要） ：实现 IPC 接收线程 ，将消息接收从 UI 线程剥离。
- 第二步 ：实施 共享内存双缓冲池 ，彻底解决内存安全和重建开销。
- 第三步 ： Controller 模式重构 ，将 ForceView 瘦身，逻辑移入 Controller。
- 第四步 ： GPU 渲染升级 （如果节点数 > 10,000），否则当前的 CPU 优化（NumPy + Qt）通常足够。

单进程多视图